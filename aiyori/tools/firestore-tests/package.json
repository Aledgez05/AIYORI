{
  "name": "aiyori-firestore-tests",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "test": "mocha --exit --timeout 20000",
    "test:emulator": "npx -y firebase-tools@latest emulators:exec --only firestore \"mocha --exit --timeout 20000\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^2.0.0",
    "mocha": "^10.0.0",
    "chai": "^4.3.7"
  }
}
