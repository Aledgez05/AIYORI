{
  "name": "aiyori-functions",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "18"
  },
  "scripts": {
    "build": "echo \"No build step\"",
    "serve": "npx -y firebase-tools@latest emulators:start --only functions,firestore",
    "deploy": "npx -y firebase-tools@latest deploy --only functions",
    "link-local": "node scripts/link_patient_local.js",
    "migrate-pins-local": "node scripts/migrate_pins_local.js"
  },
  "dependencies": {
    "firebase-admin": "^11.10.0",
    "firebase-functions": "^4.4.0",
    "bcryptjs": "^2.4.3"
  }
}
